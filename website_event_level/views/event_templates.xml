<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="website_event.event_location" model="ir.ui.view">
        <field name="active">False</field>
    </record>

<!-- Filter - Course -->
<template id="event_level" inherit_id="website_event.index_topbar" active="True" customize_show="True" name="Filter by Level">
    <xpath expr="//ul[hasclass('o_wevent_index_topbar_filters')]" position="inside">
        <li class="nav-item dropdown mr-2 my-1 my-lg-0">
            <a href="#" role="button" class="btn dropdown-toggle" data-toggle="dropdown">
                <i class="fa fa-folder-open"/>
                <t t-if="current_level" t-esc="current_level.name"/>
                <t t-else="">All Levels</t>
            </a>
            <div class="dropdown-menu">
                <t t-foreach="levels" t-as="level">
                    <t t-if="level['level_id']">
                        <a t-att-href="keep('/event', level=level['level_id'][0])" t-attf-class="dropdown-item d-flex align-items-center justify-content-between #{searches.get('level') == str(level['level_id'] and level['level_id'][0]) and 'active'}">
                            <t t-esc="level['level_id'][1]"/>
                            <span t-esc="level['level_id_count']" class="badge badge-pill badge-primary ml-3"/>
                        </a>
                    </t>
                </t>
            </div>
        </li>
    </xpath>
</template>

<template id="event_program" inherit_id="website_event.index_topbar" active="True" customize_show="True" name="Filter by Program">
    <xpath expr="//ul[hasclass('o_wevent_index_topbar_filters')]" position="inside">
        <li class="nav-item dropdown mr-2 my-1 my-lg-0">
            <a href="#" role="button" class="btn dropdown-toggle" data-toggle="dropdown">
                <i class="fa fa-folder-open"/>
                <t t-if="current_program" t-esc="current_program.name"/>
                <t t-else="">All Programs</t>
            </a>
            <div class="dropdown-menu">
                <t t-foreach="programs" t-as="program">
                    <t t-if="program['program_id']">
                        <a t-att-href="keep('/event', program=program['program_id'][0])" t-attf-class="dropdown-item d-flex align-items-center justify-content-between #{searches.get('program') == str(program['program_id'] and program['program_id'][0]) and 'active'}">
                            <t t-esc="program['program_id'][1]"/>
                            <span t-esc="program['program_id_count']" class="badge badge-pill badge-primary ml-3"/>
                        </a>
                    </t>
                </t>
            </div>
        </li>
    </xpath>
</template>

<template id="event_company" inherit_id="website_event.index_topbar" active="True" customize_show="True" name="Filter by Region">
    <xpath expr="//ul[hasclass('o_wevent_index_topbar_filters')]" position="inside">
        <li class="nav-item dropdown mr-2 my-1 my-lg-0">
            <a href="#" role="button" class="btn dropdown-toggle" data-toggle="dropdown">
                <i class="fa fa-folder-open"/>
                <t t-if="current_company" t-esc="current_company.name"/>
                <t t-else="">All Regions</t>
            </a>
            <div class="dropdown-menu">
                <t t-foreach="companies" t-as="company">
                    <t t-if="company['company_id'] and company['company_id'][0] != 'all'">
                      <t t-set="region" t-value="request.env['res.company'].sudo().browse(company['company_id'][0])" />
                      <t t-if="region.parent_id == request.website.sudo().company_id">
                        <a t-att-href="keep('/event', company=company['company_id'][0])" t-attf-class="dropdown-item d-flex align-items-center justify-content-between #{searches.get('company') == str(company['company_id'] and company['company_id'][0]) and 'active'}">
                            <t t-esc="company['company_id'][1]"/>
                            <span t-esc="company['company_id_count']" class="badge badge-pill badge-primary ml-3"/>
                        </a>
                    </t>
                  </t>
                </t>
            </div>
        </li>
    </xpath>
</template>

<template id="event_period" inherit_id="website_event.index_topbar" active="True" customize_show="True" name="Filter by Event Period">
    <xpath expr="//ul[hasclass('o_wevent_index_topbar_filters')]" position="inside">
        <li class="nav-item dropdown mr-2 my-1 my-lg-0">
            <a href="#" role="button" class="btn dropdown-toggle" data-toggle="dropdown">
                <i class="fa fa-folder-open"/>
                <t t-if="current_period" t-esc="current_period.name"/>
                <t t-else="">All Periods</t>
            </a>
            <div class="dropdown-menu">
                <t t-foreach="period" t-as="periods">
                    <t t-if="period['period_id']">
                        <a t-att-href="keep('/event', period=period['period_id'][0])" t-attf-class="dropdown-item d-flex align-items-center justify-content-between #{searches.get('period') == str(period['period_id'] and period['period_id'][0]) and 'active'}">
                            <t t-esc="period['period_id'][1]"/>
                            <span t-esc="period['period_id_count']" class="badge badge-pill badge-primary ml-3"/>
                        </a>
                    </t>
                </t>
            </div>
        </li>
    </xpath>
</template>

<template id="event_description_full" inherit_id="website_event.event_description_full">
    <xpath expr="//div[hasclass('container')]" position="replace">
            <div class="container overflow-hidden">
                <div class="row no-gutters mt-n4 mb-3">
                    <!-- Description -->
                    <div id="o_wevent_event_main_col" class="col-lg-8 bg-white px-3 pt-5 pb-0 shadow-sm">
                        <span t-raw="event.description" itemprop="description"/>
                    </div>
                    <div class="col-lg-4 bg-light shadow-sm d-print-none">
                        <!-- Date & Time -->
                        <div class="o_wevent_sidebar_block">
                            <h6 class="o_wevent_sidebar_title">Date &amp; Time</h6>
                            <div class="d-flex">
                                <h5 t-field="event.date_begin" class="my-1 mr-1 oe_hide_on_date_edit" t-options="{'date_only': 'true', 'format': 'EEEE'}"/>
                                <h5 class="my-1 oe_hide_on_date_edit" t-field="event.date_begin" t-options="{'date_only': 'true', 'format': 'long'}" itemprop="startDate" t-att-datetime="event.date_begin"/>
                            </div>
                            <t t-if="not event.is_one_day">Start -</t>
                            <span t-field="event.date_begin" t-options="{'time_only': 'true', 'format': 'short'}"/>
                            <t t-if="event.is_one_day">
                                <i class="fa fa-long-arrow-right mx-1"/>
                                <span t-field="event.date_end" t-options="{'time_only': 'true', 'format': 'short'}"/>
                            </t>
                            <t t-else="">
                                <i class="fa fa-long-arrow-down d-block text-muted mx-3 my-2" style="font-size: 1.5rem"/>
                                <div class="d-flex">
                                    <h5 t-field="event.date_end" class="my-1 mr-1 oe_hide_on_date_edit" t-options="{'date_only': 'true', 'format': 'EEEE'}"/>
                                    <h5 class="my-1 oe_hide_on_date_edit" t-field="event.date_end" t-options="{'date_only': 'true', 'format': 'long'}"/>
                                </div>
                                <t t-if="not event.is_one_day">End -</t>
                                <span t-field="event.date_end" t-options="{'time_only': 'true', 'format': 'short'}"/>
                            </t>
                            <!-- Timezone -->
                            <small t-esc="event.date_tz" class="d-block my-3 text-muted"/>

                            <div class="dropdown">
                                <i class="fa fa-calendar mr-1"/>
                                <a href="#" role="button" data-toggle="dropdown">Add to Calendar</a>
                                <div class="dropdown-menu">
                                    <a t-att-href="iCal_url" class="dropdown-item">iCal/Outlook</a>
                                    <a t-att-href="google_url" class="dropdown-item" target="_blank">Google</a>
                                </div>
                            </div>
                        </div>
                        <!-- Location -->
                        <div t-if="event.address_id" class="o_wevent_sidebar_block">
                            <h6 class="o_wevent_sidebar_title">Location</h6>
                            <h4 t-field="event.address_id" class="" t-options="{                                 &quot;widget&quot;: &quot;contact&quot;,                                 &quot;fields&quot;: [&quot;name&quot;]                             }"/>
                            <div itemprop="location" class="mb-2" t-field="event.address_id" t-options="{                                 &quot;widget&quot;: &quot;contact&quot;,                                 &quot;fields&quot;: [&quot;address&quot;],                                 &quot;no_marker&quot;: True                             }"/>
                            <div class="mb-3" t-field="event.address_id" t-options="{                                 &quot;widget&quot;: &quot;contact&quot;,                                 &quot;fields&quot;: [&quot;phone&quot;, &quot;mobile&quot;, &quot;email&quot;]                             }"/>
                            <i class="fa fa-map-marker fa-fw" role="img"/>
                            <a t-att-href="event._google_map_link()" target="_blank">Get the direction</a>
                        </div>
                        <!-- Organizer -->
                        <div t-if="event.organizer_id" class="o_wevent_sidebar_block">
                            <h6 class="o_wevent_sidebar_title">Organizer</h6>
                            <h4 t-field="event.organizer_id"/>
                            <div itemprop="location" t-field="event.organizer_id" t-options="{'widget': 'contact', 'fields': ['phone', 'mobile', 'email']}"/>
                        </div>
                        <!-- Social -->
                        <div class="o_wevent_sidebar_block">
                            <h6 class="o_wevent_sidebar_title">SHARE</h6>
                            <p class="mb-2">Find out what people see and say about this event, and join the conversation.</p>
                            <t t-snippet-call="website.s_share">
                                <t t-set="_no_title" t-value="True"/>
                                <t t-set="_classes" t-valuef="o_wevent_sidebar_social mx-n1"/>
                                <t t-set="_link_classes" t-valuef="o_wevent_social_link"/>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
    </xpath>
</template>

<template id="registration_template" inherit_id="website_event.registration_template">
    <xpath expr="//div[@id='o_wevent_tickets']" position="replace">
        <div id="o_wevent_tickets" class="bg-white shadow-sm o_wevent_js_ticket_details" data-folded-by-default="0">
            <t t-set="country_code" t-value="request.session['geoip']['country_code']"/>
            <t t-set="country" t-value="request.env['res.country'].search([('code','=',country_code)])"/>
            <t t-set="tickets" t-value="event.sudo().event_ticket_ids.filtered(lambda ticket: not ticket.is_expired)"/>
            <t t-if="country">
              <t t-set="tickets" t-value="event.sudo().event_ticket_ids.filtered(lambda ticket: not ticket.is_expired and country.id in ticket.country_group_ids.mapped('country_ids').ids)"/>
            </t>
            <!-- If some tickets expired and there is only one type left, we keep the same layout -->
            <t t-if="len(event.event_ticket_ids) &gt; 1 or tickets.description">
              <t t-if="country_code != 'LBB'">
                <div class="d-flex align-items-center py-2 pl-3 pr-2 border-bottom">
                    <span class="py-2 o_wevent_registration_title text-left">Tickets</span>
                    <div class="o_wevent_price_range d-none">
                      <t t-if="event.event_ticket_ids[-1].price_reduce &gt; 0">
                          <span class="text-dark">
                              From
                              <span t-esc="event.event_ticket_ids[0].price_reduce" t-options="{'widget': 'monetary', 'display_currency': website.pricelist_id.currency_id}"/>
                              <t t-if="event.event_ticket_ids[-1].price_reduce != event.event_ticket_ids[0].price_reduce">
                                  to
                                  <span t-esc="event.event_ticket_ids[-1].price_reduce" t-options="{'widget': 'monetary', 'display_currency': website.pricelist_id.currency_id}"/>
                              </t>
                          </span>
                      </t>
                    </div>
                    <t t-call="website_event.registration_configure_tickets_button" t-if="request.env.user.has_group('event.group_event_manager')"/>
                    <span t-if="not event.event_registrations_open" class="text-danger">
                        <i class="fa fa-ban mr-2"/>Sold Out
                    </span>
                    <a href="#" role="button" class="o_wevent_registration_btn d-none" data-target="#o_wevent_tickets_collapse">
                        <span>Tickets</span>
                        <span class="btn p-0 close d-none">×</span>
                    </a>
                </div>
                <div id="o_wevent_tickets_collapse" class="collapse show">
                    <div t-foreach="tickets" t-as="ticket" class="row px-3 py-3 mx-0 bg-light border-bottom">
                        <div class="col-md-8 col-xs-12 p-0" itemscope="itemscope" itemtype="http://schema.org/Offer">
                            <h5 itemprop="name" t-field="ticket.name" class="my-0"/>
                            <t t-if="ticket.description">
                                <small t-field="ticket.description" class="text-muted py-2"/>
                                <br/>
                            </t>
                            <small t-if="ticket.end_sale_datetime and ticket.sale_available and not ticket.is_expired" class="text-muted mr-3" itemprop="availabilityEnds">Sales end on
                                <span itemprop="priceValidUntil" t-esc="ticket.end_sale_datetime" t-options="{'widget': 'datetime', 'tz_name': event.date_tz, 'format': 'short'}"/>
                                <span t-esc="ticket.event_id.date_tz"/>
                            </small>
                            <small t-if="ticket.start_sale_datetime and not ticket.sale_available and not ticket.is_expired" class="text-muted mr-3" itemprop="availabilityEnds">
                                Sales start on <span itemprop="priceValidUntil" t-esc="ticket.start_sale_datetime" t-options="{'widget': 'datetime', 'tz_name': event.date_tz, 'format': 'short'}"/>
                                <span t-esc="ticket.event_id.date_tz"/>
                            </small>
                        </div>
                        <div class="col-md-4 col-xs-12 p-0 d-flex align-items-center justify-content-between">
                            <div class="o_wevent_registration_multi_select">
                              <t t-if="ticket.price">
                                  <t t-if="(ticket.price-website.get_current_pricelist().currency_id._convert(ticket.price_reduce, event.company_id.sudo().currency_id, event.company_id, datetime.date.today())) &gt; 1 and website.get_current_pricelist().discount_policy == 'without_discount'">
                                      <del class="text-danger mr-1" t-field="ticket.price" t-options="{'widget': 'monetary', 'from_currency': event.company_id.sudo().currency_id, 'display_currency': website.get_current_pricelist().currency_id}"/>
                                  </t>
                                  <span t-field="ticket.price_reduce" t-options="{'widget': 'monetary', 'display_currency': website.pricelist_id.currency_id}" groups="account.group_show_line_subtotals_tax_excluded"/>
                                  <span t-field="ticket.price_reduce_taxinc" t-options="{'widget': 'monetary', 'display_currency': website.pricelist_id.currency_id}" groups="account.group_show_line_subtotals_tax_included"/>
                                  <span itemprop="price" class="d-none" t-esc="ticket.price"/>
                                  <span itemprop="priceCurrency" class="d-none" t-esc="website.pricelist_id.currency_id.name"/>
                              </t>
                              <span t-else="" class="font-weight-bold text-uppercase">Free</span>
                            </div>
                            <div class="w-auto ml-auto">
                                <select t-if="not ticket.is_expired and ticket.sale_available" t-attf-name="nb_register-#{ticket.id}" class="custom-select">
                                    <t t-set="seats_max_ticket" t-value="(not ticket.seats_limited or ticket.seats_available &gt; 9) and 10 or ticket.seats_available + 1"/>
                                    <t t-set="seats_max_event" t-value="(not event.seats_limited or event.seats_available &gt; 9) and 10 or event.seats_available + 1"/>
                                    <t t-set="seats_max" t-value="min(seats_max_ticket, seats_max_event)"/>
                                    <t t-foreach="range(0, seats_max)" t-as="nb">
                                        <option t-esc="nb" t-att-selected="len(ticket) == 0 and nb == 0 and 'selected'"/>
                                    </t>
                                </select>
                                <div t-else="" class="text-danger">
                                    <span t-if="not ticket.sale_available and not ticket.is_expired and ticket.is_launched()">Sold Out</span>
                                    <span t-if="ticket.is_expired">Expired</span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="row no-gutters">
                        <div class="col-md-4 offset-md-8 py-2 pl-md-0 pr-md-2">
                            <button type="submit" class="btn btn-primary o_wait_lazy_js btn-block a-submit" disabled="" t-attf-id="#{event.id}">
                                Register
                                <t t-if="event.seats_limited and event.seats_max and event.seats_available &lt;= (event.seats_max * 0.2)">
                                    (only <t t-esc="event.seats_available"/> available)
                                </t>
                            </button>
                        </div>
                    </div>
                </div>
                </t>
          <i class="">
            <h5 style="padding-left: 20px; text-align:center; padding: 5px;"> Interested in registering in our courses or purchasing study materials? <br/> Reach out to our team and they will support you every step of the way! <br/> Tel: +961 490 830-40-50 / Email: <a href="mailto:info.beirut@morganintl.com">info.beirut@morganintl.com</a>&amp;nbsp;<br/>
            </h5>
          </i>
          <br/>
            </t>
              <div t-else="" class="o_wevent_registration_single">
                <t t-if="country_code != 'LBB'">
                  <div class="row p-2 pl-3">
                      <div class="col-lg-8 d-flex flex-columns align-items-center" itemscope="itemscope" itemtype="http://schema.org/Offer">
                          <h6 itemprop="name" class="my-0 pr-3 border-right text-dark o_wevent_single_ticket_name">
                              <span t-if="tickets" t-field="tickets.name"/>
                              <span t-else="">Registration</span>
                          </h6>
                      <div class="px-2 text-dark d-flex align-items-center align-self-stretch">
                          <t t-if="tickets.price">
                              <t t-if="(tickets.price-website.get_current_pricelist().currency_id._convert(tickets.price_reduce, event.company_id.sudo().currency_id, event.company_id, datetime.date.today())) &gt; 1 and website.get_current_pricelist().discount_policy == 'without_discount'">
                                  <del class="text-danger mr-1" t-field="tickets.price" t-options="{'widget': 'monetary', 'from_currency': event.company_id.sudo().currency_id, 'display_currency': website.get_current_pricelist().currency_id}"/>
                              </t>
                              <span t-field="tickets.price_reduce" t-options="{'widget': 'monetary', 'display_currency': website.pricelist_id.currency_id}" groups="account.group_show_line_subtotals_tax_excluded"/>
                              <span t-field="tickets.price_reduce_taxinc" t-options="{'widget': 'monetary', 'display_currency': website.pricelist_id.currency_id}" groups="account.group_show_line_subtotals_tax_included"/>
                              <span itemprop="price" class="d-none" t-esc="tickets.price"/>
                              <span itemprop="priceCurrency" class="d-none" t-esc="website.pricelist_id.currency_id.name"/>
                          </t>
                          <span t-else="" class="font-weight-bold text-uppercase">Free</span>
                      </div>
                          <small t-if="tickets.end_sale_datetime and tickets.sale_available and not tickets.is_expired" class="text-muted mr-3" itemprop="availabilityEnds">Sales end on
                              <span itemprop="priceValidUntil" t-esc="tickets.end_sale_datetime" t-options="{'widget': 'datetime', 'tz_name': event.date_tz, 'format': 'short'}"/>
                              <span t-esc="tickets.event_id.date_tz"/>
                          </small>
                          <t t-call="website_event.registration_configure_tickets_button" t-if="request.env.user.has_group('event.group_event_manager')"/>
                          <div class="ml-auto o_wevent_nowrap">
                              <t t-if="event.event_registrations_open">
                                  <span class="text-dark font-weight-bold align-middle px-2">Qty</span>
                                  <link itemprop="availability" content="http://schema.org/InStock"/>
                                  <select t-att-name="'nb_register-%s' % (tickets.id if tickets else 0)" class="w-auto custom-select">
                                      <t t-set="seats_max_ticket" t-value="(not tickets or not tickets.seats_limited or tickets.seats_available &gt; 9) and 10 or tickets.seats_available + 1"/>
                                      <t t-set="seats_max_event" t-value="(not event.seats_limited or event.seats_available &gt; 9) and 10 or event.seats_available + 1"/>
                                      <t t-set="seats_max" t-value="min(seats_max_ticket, seats_max_event) if tickets else seats_max_event"/>
                                      <t t-foreach="range(0, seats_max)" t-as="nb">
                                          <option t-esc="nb" t-att-selected="nb == 1 and 'selected'"/>
                                      </t>
                                  </select>
                              </t>
                              <t t-else="">
                                  <span itemprop="availability" content="http://schema.org/SoldOut" class="text-danger">
                                      <i class="fa fa-ban mr-2"/>Sold Out
                                  </span>
                              </t>
                          </div>
                      </div>
                      <div class="col-lg-4 pt-3 pt-lg-0 pl-2 pl-lg-0">
                          <button type="submit" class="btn btn-primary o_wait_lazy_js btn-block a-submit" t-attf-id="#{event.id}">
                              Register
                              <t t-if="event.seats_limited and event.seats_max and event.seats_available &lt;= (event.seats_max * 0.2)">
                                  (only <t t-esc="event.seats_available"/> available)
                              </t>
                          </button>
                      </div>
                  </div>
                  </t>
              </div>
        </div>
    </xpath>
</template>

</odoo>
